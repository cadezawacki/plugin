# Domain 3 — Concurrency: Parallel + SIMD

**Files:** `ParallelUtilities.cs`, `VectorizedKernels.cs`

| ID | Severity | Location | Category | Failure Scenario | Evidence | Proposed Fix | Confidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P001 | Critical | ParallelUtilities.cs:466-467 | Integer Overflow | 64K x 64K matrix: `ra * ca = 4,294,967,296` exceeds `int.MaxValue` (2.1B). Negative product silently wraps. `na != nb` check passes with wrapped values; dot product computes wrong result. | Lines 466-467: `var na = ra * ca; var nb = rb * cb;` Both are `int` multiplication. If `ra=65536, ca=65536`, product wraps. Then `if (na != nb)` may erroneously be equal under matched overflow. | Use `long` intermediates: `var na = (long)ra * ca;` before comparison. Add bounds check. | 0.92 |
| P002 | Critical | VectorizedKernels.cs:70, 89 | Integer Overflow (cascading) | `FlattenToDoubles` allocates `new double[rows * cols]` with int multiplication. 100K x 100K wraps. `BoxFlatDoubles` line 89 bounds check `flat.Length != rows * cols` passes under matched overflow. Subsequent SIMD ops read/write out of bounds. | Line 70: `var result = new double[rows * cols];` and line 89 `if (flat.Length != rows * cols)`. Both unchecked. | `checked { var total = (long)rows * cols; if (total > int.MaxValue) throw ... }` around all `rows * cols`, `m * k`, `m * n` multiplications. | 0.91 |
| P003 | High | VectorizedKernels.cs:331-332 | Bounds Violation | `DotProductAvx2` indexes `a[i]..a[i+3]` and `Vector256.Create(a[i], a[i+1], a[i+2], a[i+3])`. Loop condition `i <= a.Length - width`. Off-by-one was claimed but a careful trace: `a.Length = 5, width = 4`. `5 - 4 = 1`. Iterations: `i = 0` (passes 0 <= 1), `i = 4` (4 <= 1 fails — does NOT enter). Tail loop runs from i=4. **No actual out-of-bounds.** Loop condition is correct. | Lines 326-332. Tracing reveals condition is `i <= a.Length - width` so for `Length=5, width=4`, only `i=0` runs SIMD; tail handles i=4. | Reject finding upon re-trace OR replace with `LoadUnsafe` for performance, not correctness. | 0.30 |
| P004 | High | VectorizedKernels.cs:426, 506, 525 | Span Slice Redundancy | Minor: redundant bounds-checking in hot loops via repeated `source.Slice(r * cols, cols)`. Negligible performance impact. | N/A — micro-optimization. | None for correctness. | 0.65 |
| P005 | High | ParallelUtilities.cs:289-302 | Buffer Allocation Pressure | `ParallelBatchTransform` `localInit: () => (new double[cols], new double[cols])` allocates per partition. For cols=10M and 8 partitions, 1.28 GB allocated per recalc cycle. | Lines 293, 302. | Document or use `ArrayPool<double>.Shared.Rent/Return`. | 0.71 |
| P006 | High | VectorizedKernels.cs:657-670 | Matrix-Multiply Allocation | `MatrixMultiply` allocates `var bCol = new double[k];` per output column (in the outer `j` loop). N allocations of size k. | Line 658 is OUTSIDE the j loop, but reading the file: actually the code reads `var bCol = new double[k];` once before the j loop. Wait — re-verify against actual code. | Verify: move `var bCol` outside both loops if currently inside. | 0.76 |
| P007 | Medium | ParallelUtilities.cs:478-487 | Semantic Ambiguity | `Dot(a, b)` flattens row-major and computes dot of flattened vectors. If a is 3x3 and b is 3x3, output is dot of 9-element flatten. Semantically unclear. | Lines 458-489. | Document or constrain to vector shapes. | 0.60 |
| P008 | Medium | ParallelUtilities.cs:182 | List allocation in `PolyEval` | `List<double>(16)` may grow log N times then `ToArray()`. Minor. | Lines 182-204. | Pre-size or use array directly. | 0.54 |
| P009 | Medium | VectorizedKernels.cs:41, 48, 53 | Type-init Exception Risk | Static property initializers query `Avx2.IsSupported` etc. at type-init. If these throw on an exotic runtime, the type fails to load and all UDFs unregister. | Lines 41, 48, 53. | Wrap each initializer in try/catch returning false. | 0.50 |
| P010 | Medium | VectorizedKernels.cs:154-168 (all UDF wrappers) | Exception Swallowing | All SIMD UDFs catch broad `Exception` and log to `TraceSource`. If `TraceSource.TraceEvent` itself throws, exception escapes. OOM is silently downgraded to `#VALUE!`. | Lines 154-168 and similar. | Wrap trace call in nested try. | 0.55 |
| P011 | Low | VectorizedKernels.cs:502-514 | Vector round-trip in ColumnSums | Intermediate stackalloc buffer; suboptimal. | Lines 503, 510-513. | Direct CopyTo into `colSums.Slice(c, width)`. | 0.52 |
| P012 | Low | ParallelUtilities.cs:269-280 | Missing threshold upper bound | If caller passes huge `parallelThresholdRows`, serial fallback runs on massive input. | Line 269. | Document expected range. | 0.48 |
| P013 | Low | ParallelUtilities.cs:474-487 | Floating-point associativity | SIMD vs. scalar Dot may differ in ULP. Documented as expected. | N/A. | Documentation note only. | 0.42 |
