# Domain 4 — RTD Server + Lifetime

**Files:** `RtdServer.cs`, `ToolkitLifetime.cs` (+ cross-domain seams in `AddIn.cs`)

| ID | Severity | Location | Category | Failure Scenario | Evidence | Proposed Fix | Confidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| RTD-001 | Critical | RtdServer.cs:73 (ServerTerminate) | Resource Lifecycle / In-flight callback race | `Timer.Dispose()` returns immediately without waiting for an in-flight `FlushTick`. If the callback is mid-iteration, the subsequent `_topics.Clear()` and `FeedManager.Shutdown()` mutate state while FlushTick is reading. Stale `Topic.UpdateValue` after termination. | Line 73: `var timer = Interlocked.Exchange(ref _flushTimer, null); timer?.Dispose();` — no `WaitHandle` overload; .NET's `Timer.Dispose()` does not await pending callbacks. | Use `timer.Dispose(WaitHandle)` to await callback completion before clearing state. | 0.95 |
| RTD-002 | High | RtdServer.cs:118-139 (FlushTick) | Exception scope | Any exception in one topic's `UpdateValue` is caught by the outer `try/catch` AROUND the `foreach`, halting the whole tick and skipping every remaining topic. Sister topics get no update for that tick. | Lines 122-138: try wraps the entire foreach. | Move the try/catch inside the loop body, scoped to one topic's `UpdateValue` + `LastPushed` write. | 0.90 |
| RTD-003 | High | RtdServer.cs:326-350 (Feed.Subscribe / Unsubscribe) | Race Condition | A: writes `_subscribers[X] = regA` (no lock). B: TryRemove(Y), reads `_subscribers.IsEmpty` (sees empty if A's write not yet visible), calls Stop, cancels CTS. A then acquires lock, sees `_producer is null`, restarts producer with fresh CTS. Brief gap where A's subscription has no live producer; mild data loss. | Lines 326-328 + 343-350. | Move the dictionary mutations + IsEmpty check inside the lock. | 0.85 |
| RTD-004 | High | ToolkitLifetime.cs:54-67 (Shutdown) | Resource cleanup | `Shutdown()` calls `_cts.Cancel()` but never disposes. The CTS lingers until the next `Reset()`. If shutdown is called without a subsequent re-open, the CTS is never freed (until process exit). | Lines 54-67. | Dispose `_cts` inside Shutdown OR document that disposal happens only via Reset. | 0.80 |
| RTD-005 | High | RtdServer.cs:355-371 (Feed.Stop) | TOCTOU on CTS Dispose | `Stop()` cancels then immediately disposes the CTS, while the producer task may still be inside `await Task.Delay(token)`. Disposing a CTS that has live registrations (Task.Delay) can produce `ObjectDisposedException` in the producer's continuation. | Lines 357-370. | Cancel only; defer Dispose until the task has observed the cancellation (await the task, then dispose). | 0.75 |
| RTD-006 | High | RtdServer.cs:232-243 (FeedManager.Shutdown) | Exception safety | If any `f.Stop()` throws, the `_feeds.Clear()` line is skipped, leaving the registry inconsistent. | Lines 232-243. | Wrap in try/finally: clear in `finally`. | 0.80 |
| RTD-007 | n/a (re-traced; safe) | ToolkitLifetime.cs:25-47 | Race Condition | Re-checking the proposed race: both getter and `Reset` lock `Gate`, so `_cts` access is serialized. No race. | The lock serializes them. | None. | 0.99 (NOT a finding) |
| RTD-008 | Medium | RtdServer.cs:84-103 (ConnectData) | Logic Bug | If Excel re-issues `ConnectData` for an already-known topic ID without a `DisconnectData` in between, the new registration overwrites the old in `_topics`. The old `Feed.Subscribe` registration is orphaned: the old feed still holds the old reg in its `_subscribers`. | Line 93: `_topics[topic.TopicId] = reg;` unconditional. | Before overwriting, unsubscribe any prior registration: `if (_topics.TryGetValue(...)) oldReg.Feed.Unsubscribe(oldReg);` | 0.70 |
| RTD-009 | n/a (re-traced; safe) | RtdServer.cs:127-132 | Memory model | `LatestValue` uses Volatile.Read/Write. LastPushed is only ever written by the single flush thread. Safe. | Line 127 + 316/317. | None. | 0.95 (NOT a finding) |
| RTD-010 | Low | RtdServer.cs:412-431 (CounterFeed) | Numeric precision | After 2^53 increments the double cast loses precision. Not realistic in any workload. | Lines 427-428. | None or document. | 0.30 |
| RTD-011 | High (cross-domain) | AddIn.cs:44-52 | Ordering invariant | `ServerTerminate` already calls `FeedManager.Shutdown()`. `AutoClose` calls it again. Idempotent today but undocumented. | AddIn.cs:50-51 + RtdServer.cs:80. | Document the invariant and the redundancy; consider removing the duplicate call. | 0.65 |
| RTD-012 | Medium (cross-domain) | AddIn.cs:44-52 | Re-entrancy | If `AutoClose` is invoked twice, the second `ToolkitLifetime.Shutdown` is safe (catches `ObjectDisposedException`) but the design isn't explicit. | AddIn.cs:44-52. | Document that the close hooks must be idempotent; minimal change required. | 0.60 |
| RTD-013 | n/a (re-traced; safe) | RtdServer.cs:131, 193 | Atomicity | Reference assignment is atomic in .NET; LatestValue carries object refs. | Lines 131, 193. | None. | 0.95 (NOT a finding) |
| RTD-014 | Low | RtdServer.cs:434-455 (SineFeed) | Time-handling | Uses `DateTime.UtcNow` deltas, vulnerable to system clock adjustments. Acceptable for a demo feed. | Lines 438, 451. | Switch to `Stopwatch` for monotonic timing. | 0.40 |
| RTD-015 | Low | RtdServer.cs:314-320 | Documentation | `Volatile.Read/Write` protects only the reference. If a feed publishes a mutable object and mutates it in place, downstream readers race on the contents. No feed in this codebase does that. | Lines 316-317. | Document the contract in the Feed base class. | 0.35 |
